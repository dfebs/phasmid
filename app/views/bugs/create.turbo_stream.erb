<%= turbo_stream.prepend "bugs" do %>
    <%= render "bugs/bug", bug: @bug, project: @project %>
<% end %>

<%= turbo_stream.replace combined_dom_id(@project, Bug.new) do %>
    <%= render "bugs/new_button", bug: Bug.new, project: @project, action: "Create Bug" %>
<% end %>